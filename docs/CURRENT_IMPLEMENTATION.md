# Current Implementation

Status: **BE1–BE8 BACKEND FOUNDATION IMPLEMENTED LOCALLY; BF1–BF12 UI FROZEN**
Audit date: 2026-09-09
Owner: Engineering

## Current backend status after BE8 recovery

BE1–BE7 are durable D1-backed domains and BE8 is a read-only projection layer. BE4 persists Grooming Jobs, Hotel Stays, Daycare Attendance, lifecycle events and source-keyed Service Records. BE5 persists authority, Consent, Temporary Business QR grants and Intake with server-side scope/expiry/revoke. BE6 persists Conversations, Messages, read state, approvals, outbox attempts and webhook deduplication around a Business-owned LINE OA adapter boundary. BE7 persists Charge, Payment, allocations, refunds, provider attempts and reconciliation state while keeping service completion separate from payment. BE8 derives Reports and CRM from those canonical records without report/CRM tables.

Local validation is complete through BE8 using deterministic D1-compatible migrations and populated end-to-end records. Production Auth, real LINE credentials, a payment gateway, R2 media and Cloudflare deployment remain **NOT IMPLEMENTED**. `MEAWKETTING_FIXTURE_MODE=test` is the only supported persistent browser fixture compatibility mode; production SSR may render a deterministic, non-authoritative snapshot before D1 hydration.

## BE1 identity / Business / Branch implementation — 2026-09-06

BE1 is implemented as a local Cloudflare Worker/Vinext application boundary backed by D1-compatible SQLite. At the BE1 checkpoint it added durable `Person`, `Business`, `Branch`, `BusinessMembership`, explicit membership-to-Branch access, Branch modules/hours and append-only audit metadata. Customer/Pet was deliberately deferred then and is now implemented by BE2 below; Booking was deferred then and is now implemented by BE3. Service execution, LINE, payment, media and Consumer backends remain later phases.

| Area | Current implementation |
|---|---|
| Architecture | Frozen React UI → typed same-origin client → `POST /api/be1` → application authorization/validation → repository → D1. UI code never receives a database binding. |
| Persistence | Drizzle-defined relational schema and reproducible generated migrations for eight BE1 tables. Local development uses the D1 `DB` binding; fixtures are explicit dev/test seed data only. Branch grants retain active/inactive lifecycle and audit timestamps rather than requiring destructive deletion. |
| Person | Canonical human identity with opaque ID, active/inactive status and audit timestamps. It is not linked to Customer, Guardian or LINE identity in BE1. |
| Business / Branch | Durable profile, contact, timezone, active state, Branch contact/address, enabled modules and seven-day operating hours. A Business has many Branches; historical rows are retained when a Branch becomes inactive. |
| Membership / roles | Active/inactive Business memberships use the narrow `OWNER`, `MANAGER`, `STAFF` vocabulary. Owner has Business-wide Branch/configuration access; Manager and Staff require explicit active Branch grants. This is a foundation, not a granular permission matrix. |
| Server authorization | Every operation resolves actor Person → active membership → active Business → role/action → target Branch access. Client Business/Branch IDs are selectors only; wrong Business, inaccessible Branch, inactive membership and spoofed IDs are denied server-side. |
| Audit / correlation | Important mutations write actor, membership, Business/Branch scope, request ID, correlation ID, target and bounded before/after metadata in the same D1 batch/guarded mutation. |
| Frontend migration | BF10 text profile and Branch configuration now read/write through the typed backend client and one in-memory compatibility cache. The active context key remains a harmless session display preference; authorized workspaces and Branch truth come from the server after hydration. Legacy browser Business/Branch values are ignored and not backfilled. |
| Authentication | An explicit header-based adapter exists only when the server is configured as `dev-test`. Production authentication provider and external identity linking are **NOT IMPLEMENTED**; a production-mode request fails closed. |

Database scope now includes **BE1–BE7**; BE8 uses read-only queries over those records. Cloudflare production deployment, external providers and media/R2 are **NOT IMPLEMENTED**. Browser compatibility fixtures are explicit test-only data and are never an authoritative source for migrated Business backend domains.

## BE2 Customer / Pet implementation — 2026-09-07

BE2 extends the BE1 actor, active membership, Business authorization, application/repository and D1 patterns. It moves Business Customer and Business-local Pet relationship truth out of browser storage without changing the frozen Customers UI or starting Guardian/Passport authority.

| Area | Current implementation |
|---|---|
| Architecture | Frozen Customer/Pet screens → typed BE2 client → `POST /api/be2` → BE2 application validation/authorization → repository → the same D1 binding. The browser never queries D1 directly. |
| Persistence | Migration `0002` adds `customers`, identity-only `pets`, `business_pet_profiles`, `customer_tags` and `customer_pet_relationships`. Composite Business foreign keys prevent cross-tenant links; lifecycle checks and query-shaped indexes cover directory, phone duplicate, Pet-name duplicate and relationship traversal. |
| Customer | Stable opaque ID, Business scope, display name, optional phone/email, Business-owned notes, ordered lightweight tags, active/inactive lifecycle and actor/time metadata. One Customer record is shared across every Branch of its Business. |
| Pet | A minimal global opaque Pet anchor plus a mutable Business-local profile with name, Cat/Dog species, source, Business notes and lifecycle. The frozen UI has no real breed, sex or birth fields, so BE2 does not invent them. Avatars remain deterministic presentation; no R2 or upload system exists. |
| Relationship | `customer_pet_relationships` is a lifecycle-aware many-to-many Business contact association. It means only “this Customer is a contact/relationship for this Pet in this Business”; it never means Guardian, legal owner, Passport authority, Consent or Access Grant. |
| Search / duplicate handling | Business-scoped server queries search Customer name, normalized phone digits and linked active Pet name with limit/offset. Exact phone and Pet name/species candidates produce a non-destructive warning; staff must explicitly continue to create a separate record. Inactive identities remain warning candidates. No merge engine exists. |
| Authorization / Cross-Branch | Every BE2 operation resolves actor Person → active membership → active Business → Business-scoped target. OWNER, MANAGER and STAFF reuse the current active-membership operational foundation; Branch grants cannot expose another Business and do not fragment a Business-wide Customer/Pet directory. Wrong Business, spoofed IDs, inactive membership and cross-tenant links fail server-side. |
| Frontend migration | `/business/customers`, Customer Detail, Add/Edit Customer, Add Pet, notes, tags and search use backend truth after hydration. An in-memory compatibility cache preserves existing synchronous selectors, avatars, quick actions and remaining local Booking/Inbox/Billing/operations references by stable BE2 ID. The legacy session Customer slice is ignored, kept empty on writes and never backfilled or dual-written. |
| Passport compatibility | `be2PassportCompatibility.ts` is explicitly **DEV PROTOTYPE / NON-AUTHORITATIVE READ MODEL ONLY** for frozen seeded connection/access presentation. No Guardian, ownership, Consent, QR, protected Passport snapshot or access-grant column/API was added. New Business-local Pets default to unlinked presentation. |
| Audit / fixtures | Important mutations write actor, membership, Business, target, request/correlation ID, timestamp and bounded before/after shape in the same D1 batch. Contact values, names, notes and tag labels are not copied into audit JSON. `seed-be2-dev.sql` deterministically preserves legacy Customer/Pet IDs for dev/test only and never ingests session storage. |

At its own checkpoint BE2 intentionally stopped before Booking persistence; BE3 now supersedes that one exclusion. BE2 still does not implement service operations, Guardian, Passport/Consent, LINE, Payment, media or deployment.

## BE3 Booking / Calendar / Resource implementation — 2026-09-07

BE3 extends the same BE1 actor/membership and BE2 Customer/Pet architecture. It makes Booking planning durable without changing the frozen Calendar design or merging planning status into Grooming Job, Hotel Stay or Daycare Attendance execution.

| Area | Current implementation |
|---|---|
| Architecture | Frozen Calendar/Home/Customer projections → typed BE3 client/cache → `POST /api/be3` → BE3 application validation/authorization → repository → the same D1 binding. Browser code never queries D1 or supplies trusted availability. |
| Persistence | Migration `0003` adds ten normalized tables: planning services and their required Resource kinds, Resources and service links, Resource availability windows, Bookings, Booking↔Pet links, Resource assignments, a write-time reservation ledger and aggregate commit markers. Composite Business/Branch foreign keys prevent cross-tenant Customer/Pet/Resource links; query-shaped indexes cover Branch/range, Customer/range, module/status/range and Resource overlap. |
| Booking models | One durable aggregate supports `appointment` Grooming start/end, exclusive-checkout `date-range` Hotel stays, and `day` Daycare records. A Booking stores stable Business, Branch, Customer, service and planning state; Pet and Resource associations are normalized. The existing multi-Pet contract is preserved and capacity units count linked Pets. Current Customer/Pet/service labels are composed at read time rather than copied as identity snapshots. |
| Resource foundation | BE3 persists the schedulable projection needed for Booking: Groomer, grooming station, dryer, Hotel planning capacity and Daycare zone capacity, plus service eligibility, active state and planning availability windows. BE4 persists operation staff and Hotel room/zone execution assignments; the compatibility staff link remains distinct from Person and membership authority. |
| Availability / conflicts | Server preview and every mutation evaluate active Branch, enabled module, service, active Customer/Pets, active Customer↔Pet relationships, time shape, required/eligible Resources, Branch hours, Resource/compatibility-staff windows, exact duplicate fingerprint, exclusive overlap and per-day multi-Pet capacity. Expected business conflicts return typed recovery data instead of a generic 500. |
| Concurrency | Availability preview is advisory. Create/update stages the Booking aggregate, Pet links, assignments, reservations, commit marker and bounded audit in one D1 `batch()`. SQLite/D1 writer serialization plus reservation `BEFORE INSERT` triggers make a competing exclusive/capacity write observe the winner and abort the losing batch. Updates additionally use `WHERE revision = ?` optimistic compare-and-swap and a write token so stale edits return `VERSION_CONFLICT`. No distributed lock or client-side lock is claimed. |
| Idempotency | Create requires a Business-scoped idempotency key and stores a SHA-256 hash of the normalized request. Same key/same request returns the prior Booking; same key/different request returns `IDEMPOTENCY_KEY_REUSED`; the unique Business/key index also resolves racing retries. Edit/reschedule/resource commands use expected revision rather than a general mutation idempotency key; cancelling an already-cancelled Booking replays the current record. |
| Queries | Business-authorized list supports optional Branch, Customer, date/time overlap range, module, status and Resource filters with limit/offset; get and Branch catalogue queries remain scoped. Day/Week/Month/Custom Calendar ranges, including 28/35/42-day layouts, query one logical Booking per aggregate. Hotel is not expanded into daily Booking rows. |
| Frontend migration | The Business shell hydrates the permitted Business Booking directory page-by-page and each accessible Branch catalogue into memory. Frozen synchronous selectors project authoritative BE3 views; Calendar commands, drag/drop, both-edge resize and cancel send server mutations and use returned records/conflicts. Home and Customer upcoming Booking data derive from the same cache. Legacy `sessionStorage.bookings` is ignored and written empty; there is no browser backfill or dual write. |
| Execution boundary | BE3 may refresh a compatibility cache after a successful planning write, but BE4 D1 ServiceExecution records are the authoritative Grooming/Hotel/Daycare execution source. The cache is never a source of truth. |
| Authorization / audit | Every query or command resolves active Person → active Membership → Business → permitted Branch → target Booking/Customer/Pet/Resource. Wrong Business/Branch, foreign identities, inactive membership/grant and spoofed targets fail server-side. Create, update/reschedule, Resource change and cancel write actor, membership, Business/Branch, target, request/correlation ID, timestamp and bounded structural before/after metadata without duplicating Customer contact or notes in audit JSON. |
| Time limitation | BE3 deliberately stores validated Branch-local ISO-like values and comparable “civil minute” integers. The BE1 IANA timezone is not yet used to convert appointments to UTC instants or resolve DST/offset changes. Current hours enforcement checks the start weekday and applies open/close times only to appointments; date-range/day per-day opening policy remains an open production rule. |

The BE3 implementation is present locally. Final PASS evidence and exact command/browser results belong to [VALIDATION](./VALIDATION.md); this document does not invent results before that evidence is recorded.

## BE4–BE8 implementation — 2026-09-09

| Phase | Current implementation |
|---|---|
| **BE4 Service Operations** | D1 `service_executions`, assignments, Hotel spaces, staff, care tasks/events and source-keyed `service_records`/revisions. Grooming, Hotel and Daycare transitions are server-guarded; completion is independent from payment. |
| **BE5 Consent / Intake** | D1 `pet_authorities`, `consents`, `access_grants`, scoped fields/events, `business_intakes` and corrections. Temporary Business QR is opaque and hash-validated; expiry/revoke is enforced on every protected read/action. |
| **BE6 Inbox / LINE foundation** | D1 Conversations, Messages, reads, structured approvals, Business-owned channel metadata, outbox/attempts, external identities and webhook ledger. The adapter is provider-neutral; tests use a mock and no real LINE credentials are configured. |
| **BE7 Billing / Payments** | D1 Charges/items/events, Payments/allocations, refunds, payment attempts/provider references, webhook ledger and reconciliation. Staff/Owner/Manager policy is Branch-scoped; service completion never auto-pays. |
| **BE8 Reports / History / CRM** | Read-only queries over BE1–BE7 canonical records. Revenue is derived from Payment allocations less refunds, balances from Charge state, and CRM timeline/segments from service, booking, payment and message records. No BE8 tables or writable projections exist. |

Normal Business runtime hydrates these domains through typed clients. `MEAWKETTING_FIXTURE_MODE=test` gates persistent compatibility fixtures for deterministic tests only; a deterministic pre-hydration SSR snapshot is presentation-only, and browser/session state is not authoritative.

## Final Business UI audit

**Historical frozen-baseline note.** Business Font = LINE Seed Sans TH (local 400/700 WOFF2). The final UI correction preserved existing routes and Business domain rules. It fixed compact navigation through 1199px, dialog focus/stacking, Calendar shortcut scope and minute-accurate overlapping appointment layout, visible Booking statuses, Grooming feedback/focus and readable lanes, Inbox draft isolation, mobile Reports rows, Settings footer overlap, input sizes and prototype-facing copy. Two seeded Business contact emails no longer expose a `demo` marker; relationship rules were unchanged. That frontend audit itself changed no backend, Consumer or `/workfiledesign` implementation; BE1–BE3 were added afterward without rewriting the frozen UI. See VALIDATION for commands and current evidence.

## BE0 backend readiness — 2026-09-05

The frozen BF1–BF12 frontend was audited for production boundaries and documented in [BACKEND_READINESS](./BACKEND_READINESS.md). The document maps browser-local fixtures/session stores to persistence, derived, ephemeral and external categories; locks the application/domain/persistence separation; and records Cloudflare, LINE, payment, media, privacy, migration and TypeScript decision gates. **This paragraph and the block below are the historical BE0 snapshot, superseded for backend/database scope by the current BE1–BE8 sections above:** Backend and Database were not implemented at BE0; Production Auth, LINE and Payment Gateway remain not implemented. No API, database, identity, provider, deployment, Consumer or `/workfiledesign` code was added or changed by BE0 itself.

```text
HISTORICAL BE0 SNAPSHOT — LOCAL ONLY / BROWSER-LOCAL MOCK STATE
REAL AUTHENTICATION, AUTHORIZATION AND BUSINESS MEMBERSHIP: NOT IMPLEMENTED
PAYROLL / HR / WORKFORCE SCHEDULING: NOT IMPLEMENTED
REAL TOKEN SECURITY: NOT IMPLEMENTED
BACKEND AND DATABASE: NOT STARTED
REAL PAYMENT PROCESSING: NOT IMPLEMENTED
FULL ACCOUNTING / TAX / REFUND POLICY / CROSS-BRANCH SETTLEMENT: NOT IMPLEMENTED
TARGET PLATFORM: Cloudflare
PRODUCTION: NOT DEPLOYED / NOT VERIFIED
PRODUCTION READY: NO
BUSINESS DESIGN SYSTEM: ATTACHED REFERENCE MAPPED INTO CURRENT ARCHITECTURE
BUSINESS THEME: LIGHT / WARM WHITE ONLY
BUSINESS FONT: LINE SEED SANS TH — ACTUAL LOADED FACES 400 / 700; MAX UI WEIGHT 700
CONSUMER FONT: UNCHANGED / PAUSED
CONSUMER CURRENT: STANDALONE WEB PROTOTYPE RETAINED / FROZEN
CONSUMER TARGET: LINE-FIRST GUARDIAN EXPERIENCE THROUGH LINE MINI APP
CONSUMER DEVELOPMENT: PAUSED
LINE LOGIN: NOT IMPLEMENTED
LINE MINI APP: NOT IMPLEMENTED
LINE NOTIFICATIONS: NOT IMPLEMENTED
PRODUCTION GUARDIAN IDENTITY LINKING: NOT IMPLEMENTED
```

## BF10–BF12 implementation checkpoint — 2026-09-04

- **BF10 Settings/branches:** `/business/settings` edits shared Business profile/logo/contact and Branch identity/contact, activation, enabled services and weekly hours. `?section=branches` selects the Branch section. Branch cards derive active staff count/names/capabilities and offer a context-correct Team link; service disclosure reads existing Grooming default duration and Hotel/Daycare capacity. Merged configuration selectors drive ContextSwitcher, navigation/commands, Booking, Team and Reports; fixture-only snapshots preserve deterministic initial rendering before session-state hydration.
- **BF11 Daycare:** `/business/daycare` is capability-gated and derives Pet-specific attendance from shared day Bookings. It supports date/status operations, zone capacity, shared staff assignment, guarded intake/active/pickup/checkout/completion, care events and notes. Explicit Intake, Inbox, Billing and Customer/Service Record actions reuse existing identifiers; Home, Team and Reports include shared Daycare data.
- **BF12 derived CRM:** Customers list/detail now derives lifecycle/service/follow-up segments, last visit, next Booking, unique completed visits, repeat-use signals, outstanding balance, next actions and a Booking/service/payment/message timeline. Existing tags/notes remain shared Customer data. No additional CRM route or persisted score/store was introduced; Loyalty remains clearly planned.
- At this dated BF10–BF12 checkpoint, those frontend phases added **implemented local code only** and no backend. BE1–BE3 were added later as documented above. Final lint/test/build and browser/console/responsive results belong to [VALIDATION](./VALIDATION.md) and are not claimed here. Production auth, LINE, gateway, accounting, payroll, rewards, marketing automation, AI, Consumer expansion and deployment remain unimplemented. `/workfiledesign` remains untouched.

## Repository facts

- React 19.2.6, Vinext 1.0.0-beta.8, @vitejs/plugin-rsc 0.5.34, Vite 8.0.13 and Tailwind CSS 4.2.1.
- File-based routes under `app/`; shared UI under `app/_components`; prototype state under `app/_prototype`.
- Important UI dependencies: `react-icons` and `qrcode.react`.
- Business/public Business UI uses scoped LINE Seed Sans TH; Consumer retains its existing Noto Sans Thai/Sriracha rules.
- LINE Seed Sans TH webfonts are local WOFF2 assets in `public/fonts/line-seed-th/`; runtime `@font-face` registration loads Regular 400 and Bold 700 (59,000 bytes before transfer compression). Business UI does not request or compute weights above 700.
- `@font-face` uses `font-display: swap`; FC Minimal and Anuphan are removed from Business font loading, and `font-synthesis: none` prevents synthetic Business faces.
- BE1–BE8 runtime dependencies include `drizzle-orm`; development tooling includes `drizzle-kit`, `tsx` and official Cloudflare Workers types.
- Validation commands include `npm run lint`, `npm test`, `npm run build`, the BE1–BE8 strict typechecks and test suites, and `npm run db:check`; [VALIDATION](./VALIDATION.md) owns their latest outcomes.
- Static derived manual: `docs/.htmlmanual/manual.html`; local server: `scripts/manual-server.mjs`.
- Cloudflare Worker/Vinext plus D1 is the implemented BE1–BE8 local architecture. The production authentication provider, external credentials, production resources, deployment and verification are not implemented.
- The repository contains a standalone Consumer web prototype. It is the **CURRENT / RETAINED / FROZEN** experience, not the target final Guardian channel.
- The target Guardian channel is a future LINE Mini App. No LINE Login, Mini App, LINE notification, or production Guardian identity-linking implementation exists.

## Route implementation

The current audit contains 34 `page.tsx` route entries: 30 active local routes, 3 compatibility redirects, and 1 legacy QR demo. The canonical route table is owned by [ROUTES](./ROUTES.md). BF10 adds `/business/settings`; BF11 adds capability-gated `/business/daycare`; BF12 extends the two existing Customers routes without adding a CRM route. Shared Service Records are domain data created by Grooming completion or Hotel/Daycare checkout/completion and shown in Customer/Pet detail.

No LINE Mini App route or replacement Consumer route was added.

## Implemented surfaces & components

| Area | What code supports | Implementation boundary |
|---|---|---|
| **Business Landing (`/`)** | Business-first Hero, `/business/login` primary CTA, daily-work product board, realistic pet-business photography, multi-service content, floating Warm White glass Header, selective Bento/card hierarchy, secondary Guardian bridge, and rounded public Footer with real destinations only | Live local server components; no backend claims |
| **Business Login (`/business/login`)** | Dedicated Business Login page with mock Google authentication and the scoped LINE Seed Sans TH/Warm White Business visual system | Browser-local mock auth |
| **Business App Shell (BF-1/BF-10/BF-11)** | Compact Warm White business frame with Branch switcher, live Home/Calendar/Customers/Messages/Finance/Reports/Team navigation, Inbox unread badges, capability-gated Grooming/Hotel/Daycare destinations and commands, live shared Settings, 5-slot mobile navigation/More sheet, aligned 44px top-right controls, non-blurring account popover, Ctrl/Cmd+K Command Palette beside Scan, and no marketing Footer | BE1-authorized Business/Branch configuration, BE2 Customer directory, BE3 Booking directory/Branch catalogues and BE4–BE8 typed clients hydrate the shell; active selection remains a browser preference, persistent compatibility is explicit fixture mode only, and any pre-hydration snapshot is non-authoritative |
| **Reports & Business Insights (BF-8/BE8)** | `/business/reports` single-page business dashboard; date range presets (วันนี้, 7 วัน, 30 วัน, กำหนดเอง), current-Branch vs all-Branches scope, payment-derived revenue matching BE7, completed Grooming/Hotel/Daycare services, booking statuses, active/new/returning customers, room occupancy metrics, busiest day/peak appointment times, cancellations/no-shows, work completion status, top customers and recent services table | Read-only D1 projection over BE1–BE7 canonical records; no report tables, accounting, tax, payroll, forecasting, AI analytics, or data warehouse |
| **Team & Staff Operations Foundation (BF-9)** | `/business/team` responsive directory with avatar anchors, Branch-aware shared display identity, multi-Branch assignment, Owner/Manager/Staff display roles, Grooming/Hotel care/Daycare/Front desk capabilities, active/inactive state, lightweight working/unavailable/break/time-off windows, and derived today/current/next/conflict workload. BE3 Resources may carry an opaque link to that display member; BE4 uses the durable directory for execution assignment guards | BE4 D1 operation-staff directory and availability; BF9 roles do not authorize requests and there is no full workforce scheduling, certification, attendance, payroll, HRIS, or productivity scoring |
| **Business & Branch Settings (BF-10)** | Business profile and local logo preview, main contact, Branch add/edit/contact/activation, service toggles and weekly hours; shared active Team count/names/capabilities, Branch-aware Team link, and existing service duration/room-or-zone capacity disclosure | Text profile and Branch configuration persist through the BE1 typed boundary; media bytes/R2, production Business verification and a granular permission matrix are not implemented |
| **Daycare Operations (BF-11)** | Capability-gated board/grouped mobile views, shared Booking-derived attendance per Pet, zone capacity and staff guards, care/notes, pickup, explicit Intake/Inbox/Billing links and source-keyed Service Record creation after checkout/completion | Existing Business envelope and shared identities; no medical/safety policy engine, automated matching or Payment implied by execution |
| **Customer CRM & Retention (BF-12)** | Derived overview/segments, relationship facts/signals, contextual next action and filtered unified timeline inside Customers list/detail | No extra CRM store/route, prediction, reward, campaign, automated send or AI; Loyalty is planned only |
| **Business Home (`/business/home`)** | Three-variant operational banner using existing local imagery with previous/next arrows, one transform-only track, click navigation, touch swipe, reduced-motion transition fallback, square composition and a desktop 50:50 split capped at 400×400px; attention, next work, Grooming Service Job values, Hotel arrivals/departures/occupancy/capacity/attention, Daycare attendance/pickup summary, and current-Branch payment-derived revenue/outstanding values from shared state | Upcoming/next Booking summaries use BE3 cache truth; execution and financial summaries hydrate from BE4/BE7/BE8 typed clients after the durable state is ready; visible work follows enabled Branch modules |
| **Business Calendar (BF-2)** | Shared segmented Day/Week/Month/Custom view component; 28/35/42-day custom grids; Agenda-first mobile; compact color-surfaced cards with status retained in accessible names; unobtrusive edge resize affordances; Today scroll/focus; Ctrl/Cmd copy, paste and undo; Delete/Escape/arrow/Home/End spreadsheet-like keys; Alt-drag duplication; view preference cookie; availability-guarded mutation | Frozen UI over BE3 D1 Booking truth; range reads and authoritative create/edit/reschedule/Resource/cancel results pass through the typed backend boundary |
| **Booking Foundation (BF-2/BE3)** | One-title Booking Editor sheet/dialog with Branch-enabled visual service selector, typeahead Customer combobox, shared Pet-image picker, inline Add Customer/Add Pet, clearer section spacing, aligned appointment/date-range/day fields, automatic availability/resource conflict feedback, review, cancellation and safe recovery | BE3 D1 persistence with stable BE2 references, server conflict revalidation, Business-scoped create idempotency and optimistic revisions; no browser Booking backfill or dual write |
| **Grooming Operations Foundation (BF-5/BE4)** | `/business/grooming` capability-gated Today board with compact Pet-image Job cards, no date/job filter rail, five full-surface status colors, pointer-following drag preview with valid/invalid and rollback feedback, reversible desktop/tablet status drag, touch drop/swipe and mobile grouped status-list/detail alternative, guarded Job transitions, timing, shared Resource assignment, internal notes, Inbox request action, lightweight Job history, BE7 checkout, and automatic creation/update of one shared Service Record on completion | D1 service executions/events/records through the BE4 client after hydration; Job completion remains distinct from Payment and this is not a full production Grooming system |
| **Hotel / Boarding Operations Foundation (BF-6/BE4)** | `/business/hotel` capability-gated Today operations plus continuous room/zone occupancy, occupied/reserved/available capacity, Pet-specific Stay lifecycle, guarded assign/move with dated history and rollback, lightweight authorized care, ready-for-pickup, internal notes/incidents, responsive grouped mobile alternative, shared Calendar/Home/Customer/Inbox/Intake integration, BE7 checkout, and automatic creation of one shared Stay Service Record on checkout/completion | D1 service executions/assignments/care/events/records through the BE4 client after hydration; Stay completion remains distinct from Payment and this is not a production Hotel, medical, full incident, inventory or pricing system |
| **Billing, Payments & Revenue Foundation (BF-7/BF-11)** | `/business/billing` Charge review/edit, source-aware base/add-on/manual-adjustment/discount lines, separately recorded Cash/bank-transfer/Other Payment allocations, derived unpaid/partial/paid/cancelled state, duplicate-safe submission, unpaid cancellation reason, active-Branch revenue/outstanding summary, refunds and shared Grooming/Hotel/Daycare checkout context | D1 BE7 financial model through the typed client; whole Thai Baht integer assumption only; no gateway, bank confirmation, full accounting, tax, automated refund processor, invoice compliance, or cross-Branch settlement |
| **Shared Service Record foundation (BF-8/BE4)** | Grooming completion and Hotel/Daycare checkout/completion create or update one source-keyed, Pet-specific, Branch-attributed Service Record with permitted summary/timeline/resource facts, append-only correction/source-recompletion history, and a read-only BE7 payment reference available to history | D1 BE4 service records/revisions through the typed client; no standalone CareProof module/menu/route/workflow. Service Record is not a receipt, certificate, Pet Passport, medical record, real photo store, or Guardian/LINE delivery surface. |
| **Customers & Pets (BF-3/BF-8/BF-12)** | Responsive operational rows using the shared Business search field and 14px `BusinessSegmentedControl` lifecycle/service/follow-up segments; zoned Customer detail with icon-only primary actions, inline name edit, Pet-section add action, neutral Customer/Pet avatars, mobile multiple-Pet snap/peek, progressive Passport/access disclosure, durable add Customer/Pet, tags/notes, duplicate warning, Booking preselection, current/recent Hotel Stays, compact shared Charge/Payment history, and one inline shared `ประวัติบริการ` timeline/list | Customer/Pet identity, Business-local profiles/relationships, notes/tags and search use BE2 D1 truth; upcoming/recent Booking planning uses BE3 truth; execution/financial history and CRM projections use BE4/BE7/BE8 records. Guardian authority is not implemented. |
| **Inbox & Customer Communication (BF-4/BE6)** | `/business/inbox` readable desktop split/mobile full-task UI; Business-wide Customer conversation reuse; shared search; 14px `BusinessSegmentedControl` filters (ทั้งหมด / ยังไม่ได้อ่าน / กำลังใช้บริการ); safe Customer/Pet/Booking/Branch/Grooming/Hotel context; durable send/unread, horizontal mobile quick replies, structured add-service request, query recovery and optional staff-triggered billing text | D1 BE6 Conversation/Message/read/approval/outbox records through the typed client; mock/provider-neutral delivery boundary only, with no production LINE credentials, attachments or Consumer Inbox |
| **Shared Intake & Scanner (Phase E/BF-11/BE5)** | Camera scan, neutral manual code recovery, Temporary Business QR validation, consent review, belongings logging, check-in completion, eligible Grooming Job handoff, explicit Hotel `hotelStayId` and Daycare `daycareAttendanceId` handoffs without a second Intake flow; fixture/interruption controls remain state/test-only | D1 BE5 grants/consents/intakes with server scope, expiry and revoke; target reuse checks matching Business/Branch/Customer/Pet and Daycare capacity at check-in |
| **Consumer web prototype (Current / Frozen)** | Anonymous create flow, 6 passport themes, My Pets, Public Safety, Lost flow, Temporary Business Sharing | Implemented and regression-tested; retained/frozen; feature expansion paused |
| **LINE-first Guardian experience (Target)** | LINE entry, LINE Login, LINE Mini App, LINE notifications, production Guardian identity linking | Not implemented; future Consumer phase only |
| **Homepage imagery** | Realistic local photo assets in `public/images/business` for Hero, services, workflow action, and closing entries; the public landing does not depend on cat stickers | Static generated assets only; `/workfiledesign` source untouched |
| **Shared Business visual primitives** | Semantic buttons/forms/badges/status, reusable search, keyboard segmented control, breadcrumbs/sidebar headers, semantic data table, alert, focus-managed modal, progress and skeleton components, signature sweep/rainbow modifiers, workflow-appropriate list/row/timeline/board surfaces, 44px touch controls, adaptive dialog/sheet treatment, pathname-keyed route fades, staggered region entry and reduced-motion fallbacks | Visual migration only; existing Product state and domain behavior preserved |

## Business visual system (Warm White / Pastel Yellow)

- `workfiledesign/htmlpack/index.html` and `workfiledesign/htmlpack/design-system.css` were audited and mapped as visual reference inputs; they are not runtime dependencies and `/workfiledesign` remains untouched.
- Scoped semantic Business tokens live in `app/globals.css`: Background `#FFFDF9`, Foreground `#2B2B2B`, Surface `#FFFFFF`, Border/Input `#ECE8DF`, Primary `#F4C95D`, Hover/Ring `#D7B152`, Primary Foreground `#3D2B00`, Soft Yellow `#FFD86B`, and Accent `#FFF7EB`.
- Semantic status tokens are Success `#4F7D51`, Warning `#936F28`, Critical `#AC5B53`, and Information `#507893`, with soft and dot counterparts. Status remains text plus icon/dot/non-color meaning.
- Service classification remains independent: Grooming uses Scissors + Coral `#FF9B85`, Hotel uses Bed + Sky `#6FB1E0`, Daycare uses PawPrint + Mint `#5FCFA8`, and Grape `#B79BDB` is reserved for future/special classification.
- Business/public Business uses LINE Seed Sans TH with 16px operational body text, 14px supporting text, 24–30px responsive page titles, 16px minimum mobile form text, and a hard 700 maximum weight; Consumer retains its existing Noto Sans Thai visual language. JetBrains Mono is not a general Business UI font.
- Business implements Light / Warm White only. There are no Business dark theme tokens, dark portal attributes or theme toggle.
- Emoji/Dingbat UI icons remain prohibited. Rainbow CTA styling remains reserved/experimental; multi-accent indeterminate progress is used only for structural Business loading and never for operational status.
- Component curvature follows hierarchy (buttons 8–12px, inputs around 8px, cards 17–20px, large public surfaces 20–28px); pills remain semantic rather than universal.
- Motion uses premium easing with canonical 180ms Fast, 220ms Base, 300ms Slow and 280ms Navigation tokens plus reduced-motion handling. Home banner navigation is manual; decorative public motion does not loop indefinitely. Structural Business skeletons represent metrics, rows and Calendar layout instead of one giant spinner.
- Consumer source and styling are outside this migration. **CONSUMER VISUAL REDESIGN: PAUSED UNTIL PRODUCT OWNER REOPENS.** The future Guardian visual direction is a minimal, mobile-first LINE Mini App and is not implemented here.

## Durable and prototype storage

BE1 Business/Branch, BE2 Customer/Pet, BE3 Booking/planning Resources, BE4 execution/Service Records, BE5 Consent/Intake, BE6 Inbox and BE7 financial records use D1. BE8 Reports/CRM are read-only queries. The remaining Consumer and explicitly gated fixture stores use same-tab `sessionStorage`; lightweight display preferences use browser cookies and never enter the backend source of truth:

| Key | Purpose |
|---|---|
| `meawketting:create-passport:prototype-v1` | Create/claimed-local presentation |
| `meawketting:safety-lost:prototype-v1` | Safety/Lost state |
| `meawketting:business-sharing:prototype-v1` | Temporary sharing/access state |
| `meawketting:business-intake:prototype-v1` | DEV/TEST fixture compatibility only. In normal runtime BE4/BE5 execution and Intake data are loaded from D1; fixture mode may render deterministic fallback records and is never a production backfill or security source. |
| `meawketting:business-inbox:prototype-v1` | DEV/TEST fixture compatibility only. In normal runtime BE6 Conversation/Message/read/outbox data are loaded from D1; fixture mode may render deterministic fallback records and is never an authoritative message source. |
| Calendar view preference cookie | Last selected Day/Week/Month/Custom view (and related range choice where applicable) |
| Inbox quick-reply visibility cookie | Whether the staff member collapsed or reopened quick replies |

BF12 CRM has no storage key: its profiles, segments, signals and timeline are recomputed from the existing Business and Inbox sources.

CRM's current reversible heuristics count unique completed-service Booking IDs rather than one visit per Pet record. Two visits produce a repeat/regular signal; a last visit at least 45 days before the dataset reference with no next Booking produces inactive. The reference is the newest recorded Customer/service/financial/conversation timestamp, with a clock fallback only for an empty dataset. These labels do not implement a live monitor, prediction, customer entitlement or loyalty policy.

## Current limitations

- BE1–BE7 have typed APIs, migrations and durable canonical records locally; BE8 has read-only Reports/CRM queries. No production database has been provisioned or deployed.
- BE3 time values remain validated Branch-local civil strings/minutes. They are not UTC instants, do not yet apply the stored IANA Branch timezone, and do not resolve offset/DST transitions. Appointment hours use the start weekday; complete multi-day opening/closing policy remains open.
- BE3 Resource rows remain a minimal planning projection; BE4 owns execution assignments/rooms/zones and operation staff records. Full HR/workforce policy remains outside scope.
- BE3 create idempotency and optimistic revisions, BE4–BE7 mutation idempotency, webhook/event ledgers and BE8 snapshot scoping are validated locally. Cross-store browser compatibility is explicit fixture mode only.
- No production authentication provider, account recovery, external identity linking, owner-transfer/admin portal or granular permission matrix. The explicit dev/test identity adapter is not production auth.
- Business logo upload bytes are a frozen local preview only because media/R2 is outside BE1; durable text profile/configuration is implemented.
- No Customer merge engine, Guardian invitation/linking, Customer contact authority, Pet ownership policy, Passport permission backend, or cross-Business identity federation. BE2 duplicate handling is warning-only and Business-wide Customer/Pet identity deliberately ignores Branch as an identity scope.
- Real-time sockets, attachment storage and production notifications remain unimplemented; BE6 does provide durable Message/read/outbox/webhook retry/deduplication foundations and a signed LINE adapter boundary.
- No production Guardian identity linking, LINE Login or LINE Mini App; LINE identity remains separate from Pet authority.
- No production Guardian identity linking. LINE identity is only a future entry/authentication channel and does not establish Pet ownership authority automatically.
- No real payment provider, bank confirmation, tax document, invoice-compliance, General Ledger, accounting export/integration, or cross-Branch settlement capability. BE7 records manual refunds with role/Branch authorization and immutable audit evidence.
- No full room-management settings, overbooking override, room-sharing policy, waitlist, cross-Branch Hotel transfer, housekeeping workforce scheduling, medical management, full incident system, complex staff scheduling, certification system, attendance machine, timesheets, leave approval, payroll/salary/commissions, HRIS, inventory/product-stock, pricing override/discount/refund authority, full document/certificate engine, Guardian Service Record visibility, real photo storage, or production Grooming/Hotel workflow.

## Engineering checkpoint

- The 2026-08-30 Business UX/UI reset evidence remains valid for the public Business entry, shell, Home, Calendar/Booking overlays, Customers/detail, Inbox, Scanner/Intake and Grooming across its recorded breakpoints. BF-6 was additionally checked in the in-app browser at a 1294px desktop width and 390×844 mobile: the desktop occupancy board stays contained, mobile uses grouped lists/tabs and a bottom sheet, and the non-drag room selector remains usable.

- Homepage correction completed: `/` communicates Business value in the first viewport, uses Business Login as the primary CTA, keeps Guardian secondary, labels demo/planned content honestly, and avoids duplicating `/business`.
- Public shell correction uses a 1200px centered maximum with 32px desktop, 24px tablet and 16px mobile gutters; operational Calendar/board workflows retain appropriate width. Below 768px, the existing hero photo appears immediately after the headline, before supporting copy and actions; decorative preview chrome is hidden without changing Consumer or desktop composition.
- Homepage generated imagery is photographic rather than cartoon illustration. The landing uses real pet-business photography as its visual language; CI sticker assets are not part of the public landing dependency.
- BF1–BF12 operational UI remains frozen without duplicating Customer/Pet/Booking/Conversation/Intake/financial/Team records. Customer/Pet identity comes from BE2 and Booking planning comes from BE3; execution, Intake, Inbox, finance and Service Records use BE4–BE7 D1 records while BE8 derives Reports/CRM. Shared service identity, avatars, Home Spotlight, compact shell, live Finance/Reports/Team/Settings plus capability-aware Grooming/Hotel/Daycare navigation, row/timeline/schedule/occupancy/financial/Service Record/Team workload surfaces, and restrained cards/chips remain intact. The former standalone CareProof surface is superseded.
- Customer list no longer uses the old desktop table-header structure; desktop/tablet rows use a stable concise hierarchy, mobile stacks the same priority fields, and Business-scoped name/phone/Pet search is executed through BE2. Passport connection is not a browse filter, and detail separates contact, Pets, Bookings and Business notes while keeping the non-authoritative authority/access compatibility context progressively disclosed.
- Calendar uses a Sunday-first range-aware planning board backed by BE3. Hotel stays render continuously as one logical date-range Booking; Day/Week/Month/Custom share one component/date model; mouse, pen and touch pointer drag use one interaction path for move and both-edge resize, while native attributes remain a semantic fallback. Clicking selects a Booking before copy; Enter/double-click opens it. Copy/paste, undo, Delete, arrow navigation and Alt-drag duplication all form guarded commands; the server revalidates and rejected conflicts retain/restore the authoritative Booking. Today moves, scrolls and focuses the current demo date.
- Booking create/edit keeps appointment, date-range, day, Branch/module validation, conflict, review and cancellation UX while using BE3 create/update/reschedule/assignment/cancel commands, the visual service selector, searchable BE2 Customer combobox, Pet imagery and server availability feedback. Duplicate submits reuse a create idempotency key; stale revisions recover from the current server record.
- Grooming uses D1 Service Jobs/events/records through BE4. A Grooming Booking projects a Pet-specific linked Job; Booking planning/status stays independent of the Job execution lifecycle. An explicit BE7 checkout may reference the Job, but completion never records a Payment. Home, Calendar, Scan and Intake reuse this foundation; Consumer web routes and privacy boundaries remain preserved and frozen.
- BF-4/BE6 Inbox Foundation is **IMPLEMENTED LOCALLY / PROVIDER-NEUTRAL** at `/business/inbox`; Home, Customer Detail, Booking context, Grooming Job detail, and navigation reuse D1 Conversation/Message state. Customer/Booking/Job launches reuse the Business + Customer conversation and normalize to `?conversation=`; quick replies default to three and their collapsed state is a cookie-only preference; no dynamic conversation route was needed.
- Public Header and Footer follow the attached visual direction: floating glass Header, a token-driven Signature Sweep Business login button and rounded public Footer with real navigation only. `RouteFooter` continues to exclude the marketing Footer from every logged-in Business operational route.
- BF-5/BE4 Grooming Operations Foundation is **IMPLEMENTED LOCALLY**. The capability-aware board has pointer-following mouse/pen/touch drag preview with reversible status movement and a grouped mobile list; centered plain lane headings, visible status chips and full-surface card colors communicate the queue clearly. Date/job filters are removed from the board surface. It gates transitions, persists actual start/completion timing in D1, keeps Business notes distinct from Inbox messages, validates shared Resource assignments, and contributes completed Jobs to shared Customer/Pet history. It is not full Grooming production operations.
- BF-6/BE4 Hotel / Boarding Operations Foundation is **IMPLEMENTED LOCALLY**. A shared Hotel Booking projects one linked Stay per Pet; Booking remains date-range planning while Stay owns execution lifecycle, occupancy, assignment/move history, authorized lightweight care, Business notes, lightweight attention incidents and pickup readiness. An explicit BE7 checkout may reference the Stay, but operational completion never records a Payment. Calendar, Home, Customer detail, Inbox and the Shared Intake Engine derive or link through the same identifiers instead of creating parallel stores.
- BF-7 Billing, Payments & Revenue Foundation is **IMPLEMENTED LOCALLY**. Charges and Payments live in D1 and are intentionally separate: Charges keep line-item financial snapshots; Cash, bank-transfer and Other Payment allocations derive unpaid/partial/paid/cancelled state. One Booking-level base amount is not duplicated per Pet-specific Job/Stay; approved Grooming add-ons are reconciled idempotently at checkout. Billing, Customer history, Home and BE8 Reports use the same active-Branch records.
- BF-8 shared Service Record foundation is **IMPLEMENTED LOCALLY**. One completed Grooming Job or checked-out/completed Hotel Stay/Daycare Attendance creates/updates one shared, source-keyed D1 Service Record. It retains only permitted Business service evidence, exposes a read-only BE7 payment reference in Customer/Pet history, and keeps correction/source-recompletion audit history. Service completion and `Paid` remain separate states. Standalone CareProof/Handover UI is **SUPERSEDED**; Guardian LINE visibility and real photo storage remain unimplemented.
- BF-9 Team & Staff Operations Foundation is **IMPLEMENTED LOCALLY** at `/business/team`. Operation staff, capabilities and availability are durable BE4 records and remain distinct from BE1 memberships. BE1 separately enforces the `OWNER`/`MANAGER`/`STAFF` membership vocabulary for Business/Branch configuration: Owner has Business-wide configuration access; Manager/Staff require explicit Branch grants and cannot mutate configuration. Production authentication is not implemented.
- Structured Approval is a **LOCAL PROTOTYPE FOUNDATION**. Waiting/approved/declined/cancelled states exist with duplicate-safe Guardian-side test decisions. A Guardian-approved Grooming request adds only to the linked Job's local add-ons/estimated duration; it does not mutate Booking add-ons or create Charge/Payment.
- Real Messaging, real Notifications, attachments, and the full Consumer Messages application are **NOT IMPLEMENTED**. Consumer independent development remains **PAUSED**.
- LINE Login, the LINE Mini App, notifications and production Guardian identity-linking are **NOT IMPLEMENTED**. BE6 does provide a raw-body-verified, provider-neutral Business-owned LINE OA webhook boundary; no production credentials or delivery channel are configured.
- Advanced/full Hotel policy and workforce systems remain deferred alongside advanced Daycare policy, full document/certificate Service Records, Full Incident Management, Production Auth, Guardian/Passport visibility, real payment processing/accounting, payroll/HR, LINE transport and Cloudflare production deployment. **BE1–BE8 backend foundation is implemented and validated locally; production integrations remain open.** Consumer remains PAUSED; `/workfiledesign` remains untouched.
